# GENERATED from src/registered-defs/sprint-arm-scope.yaml — do not hand-edit.
# Regenerate: node scripts/check-workflow-def-sync.mjs --write sprint-arm-scope
# The canonical fixture is a mirror of the registered-def (INF-723 single-source rule).

id: sprint-arm-scope
version: 1
archetype: single-task
entry_state: doing

break_glass:
  command: escape
  to: doing
  owner_role: steward

states:

  - id: doing
    owner_role: sprint-owner
    kind: normal
    native_state: doing
    # Produce the scope artifact: what's in, what's out, boundaries, the debt
    # candidates. Hand to charles for review.
    transitions:
      - command: submit
        to: review
        assign: { mode: auto }        # review is charles (singleton code-review)
        generic: continue

  - id: review
    owner_role: code-review           # resolves to charles
    kind: normal
    native_state: todo
    # charles reviews the scope artifact for completeness/coherence, then completes
    # the arm (done) or bounces it back to doing for revision.
    transitions:
      - command: approve
        to: done
        generic: continue
      - command: request-changes
        to: doing
        assign: { mode: auto }
        generic: revision
        feedback:
          required: true
          category_enum:
            - incomplete
            - off-brief
            - scope-creep
            - correctness

  - id: done
    kind: terminal
    native_state: done
    satisfies_parent_barrier: true   # signals the parent managing-arms barrier
